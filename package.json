{
  "name": "checkmate",
  "version": "1.0.0",
  "scripts": {
    "build": "cargo lambda build --output-format zip --x86-64 --release",
    "deploy": "npm run build && serverless deploy",
    "deploy:local": "npm run build && serverless deploy --stage local",
    "deploy:prod": "npm run build && serverless deploy --stage prod"
  },
  "devDependencies": {
    "serverless-localstack": "^1.3.1"
  }
}
